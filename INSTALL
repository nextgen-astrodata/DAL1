The Data Access Library (DAL1) uses the CMake cross-platform build system.
Follow the instructions on `http://www.cmake.org` to install CMake.

- Go to the build directory:

    mkdir build
    cd build

- Use CMake to generate platform specific Makefile's.

    cmake ..

- Build the library.

    make

- Install the library.

    make install
