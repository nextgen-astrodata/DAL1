#!${bash_executable}

## Set environment variables

export LOFARSOFT=${USG_ROOT}
. $LOFARSOFT/devel_common/scripts/init.sh

cd $LOFARSOFT

rm -rf release build
svn up
cd build
./bootstrap
make dal
cd dal
make Experimental

